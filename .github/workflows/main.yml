name: Push a new tag with minor update

on:
  push:
    branches:
      - main
env:
  PATH: Here Path
jobs:
  release:
    runs-on: ubuntu-latest
    outputs:
      output1: ${{ steps.step1.outputs.test }}
      output2: ${{ steps.step2.outputs.test }}
    steps:
      
      - uses: actions/checkout@v2
      - uses: actions-ecosystem/action-get-latest-tag@v1
        id: get-latest-tag
       
      - name: GitHub Tag Name example
        run: |
          echo "${{ steps.get-latest-tag.outputs.tag }}"

      - id: step1
        if: ${{ steps.get-latest-tag.outputs.tag == 'v0.0.0' }}
        run: | 
            echo 'test=v1.0.1'
            tag: "v1.0.1-RC"
            message: '${{ steps.bump-semver.outputs.new_version }}: PR #${{ github.event.pull_request.number }} ${{ github.event.pull_request.title }}'
      - id: step2
        if: ${{ steps.get-latest-tag.outputs.tag == 'v1.0.1-RC' }}
        run: | 
            echo 'test=v1.0.2'
            tag: "v1.0.2-RC"
            message: '${{ steps.bump-semver.outputs.new_version }}: PR #${{ github.event.pull_request.number }} ${{ github.event.pull_request.title }}'
      - name: Print Values
        run: |
          echo "${{ needs.jobs.step1.test }}"
          echo "${{ needs.jobs.step2.test }}"
       
